"""Backend package initializer.
Added during modularization so that absolute imports inside `anomaly_cv.py` work
both when importing (`from backend import anomaly_cv`) and when invoking the
script directly (`python backend/anomaly_cv.py ...`).
"""
